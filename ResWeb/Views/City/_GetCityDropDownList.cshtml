@if (ViewData["dataList"] != null)
{
    @Html.DropDownList("cityList", ViewData["dataList"] as SelectList, new { @class = "selectpicker form-control pull-righ", @title = "请选择..." })
}
else
{
    <select class="selectpicker form-control pull-righ" id="cityList" title="暂无数据" disabled></select>
}
