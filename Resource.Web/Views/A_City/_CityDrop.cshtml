@if (ViewData["dataList"] != null)
{
    @Html.DropDownList("CityCode", ViewData["dataList"] as SelectList, "全部",new { @class = "selectpicker form-control pull-righ",@id="City"})
}
else
{
    <select class="selectpicker form-control pull-righ" name="CityCode" id="City" title="全部" disabled></select>
}
