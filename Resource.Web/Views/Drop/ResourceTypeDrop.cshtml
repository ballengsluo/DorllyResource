@*@if (ViewData["dataList"] != null)
{
    @Html.DropDownList("ResourceTypeID", ViewData["dataList"] as SelectList, "全部", new { @class = "selectpicker form-control pull-righ", @id = "type" })
}
else
{
    <select class="selectpicker form-control pull-righ" name="ResourceTypeID" id="type" title="暂无数据" disabled></select>
}*@
@if (ViewBag.model == 1)
{
    @Html.DropDownList("ResourceTypeID", ViewData["dataList"] as SelectList,
    new
    {
        @class = "selectpicker form-control",
        @id = "stype",
        @required = "required",
        @title = "请选择..."
    })
}
else
{
    @Html.DropDownList("ResourceTypeID",
        ViewData["dataList"] as SelectList,
        "全部",
        new
        {
            @class = "selectpicker form-control",
            @id = "stype"
        })
}
